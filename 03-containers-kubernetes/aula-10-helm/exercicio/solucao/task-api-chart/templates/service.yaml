apiVersion: v1
kind: Service
metadata:
  name: task-api
spec:
  type: NodePort
  selector:
    app: task-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: 30500
